# v2.0.3 Release Audit

Date: 2026-08-04

## Result

All three v2.0.3 requirements were implemented and exercised against the
repository and its real local SQLite investigation store.

## 1. `.env` permissions

A fresh CLI configure run of:

```text
python -m voidaccess_cli.main --no-banner configure tor --host 127.0.0.1 --port 9050
```

exited `0`. The real Windows ACL check reported only the current user with
full control for both `.env` and `~/.voidaccess/config.json`:

```text
.env LAPTOP-NE73JQF0\Katriel Delzyn Moses:(F)
config.json LAPTOP-NE73JQF0\Katriel Delzyn Moses:(F)
```

This is the Windows owner-only equivalent of the existing POSIX `0600` check;
no inherited or broad-user ACE remains.

The same enforcement is also called after `.env` creation and subsequent
updates in `setup.sh` (`chmod 600`) and `setup.bat` (`icacls` inheritance
removal plus current-user grant). `bash -n setup.sh` passes.

## 2. Fresh typed-relationship yield

These are five new CLI investigations, not reads from existing investigation
rows. They used the real configured LLM path, live clearnet sources, `--no-tor`,
`--depth shallow`, and did not use `--no-llm`.

| Investigation | Query | Pages | Entities | Typed edges |
|---|---|---:|---:|---:|
| `cd91d715…` | LockBit ransomware infrastructure indicators | 148 | 280 | 0 |
| `0d912e5b…` | CVE-2024-3094 XZ Utils backdoor | 27 | 139 | 0 |
| `d0f9f7ff…` | BlackCat ransomware leak infrastructure | 23 | 92 | 0 |
| `c7621c32…` | Raccoon Stealer malware campaign | 23 | 135 | 0 |
| `9f27ae35…` | AiTM MFA phishing kit indicators | 29 | 173 | 0 |
| **Total** |  | **250** | **819** | **0** |

The configured OpenRouter account rejected the LLM requests with HTTP `402`
(insufficient remaining credits, including the 16,000-token request ceiling).
All five investigations nevertheless completed through real collection and
entity persistence. Therefore this is a repeatable observed zero under the
available account state, but it is not a clean extractor-ceiling measurement:
the common LLM failure is a confounder. The prior positive 18-edge result is
not promoted to a new baseline, and no positive conclusion is forced.

## 3. Legacy-data backfill

Utility: `scripts/backfill_legacy_data.py`, implemented by
`voidaccess.backfill`.

The real run reported:

```json
{
  "typed_relationship_pages": {"scanned": 3, "resolved": 3, "unresolved": 0},
  "community_partitions": {"scanned": 84, "persisted": 84, "skipped": 0}
}
```

The three missing typed-edge page IDs on investigation `8babf3e…` now resolve
to real pages (`348d0517…`, `33399feb…`, and `33399feb…`). The five previously
audited investigations now serve persisted non-empty partitions:

| Investigation | Persisted community count | HTTP graph |
|---|---:|---|
| `8babf3e…` | 188 | `200`, `complete` |
| `b3c755b…` | 202 | `200`, `complete` |
| `7aa20b9…` | 185 | `200`, `complete` |
| `cfa5147…` | 185 | `200`, `complete` |
| `06dfebb…` | 91 | `200`, `complete` |

The HTTP response community maps contained real node assignments, and their
`community_count` values matched the persisted partitions.

## Verification

The focused v2.0.3 checks passed (`53 passed`). The clean v2.0.2 stash-and-compare
baseline passed (`1291 passed, 7 warnings`). The post-change full suite passed
`1294 passed, 7 warnings` in 209.03 seconds. The three additional passing tests
are the new backfill coverage.
