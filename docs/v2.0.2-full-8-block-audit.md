# VoidAccess full eight-block audit

**Date:** 2026-08-04 (Asia/Calcutta)  
**Audited head:** `e19f804e` / v2.0.2  
**v2.0.1 reference:** `e7885a04` / tag v2.0.1

## Executive result

The requested v2.0.1 blocker re-checks, live Tor ControlPort proof, live priority-score parity, typed-relationship yield, exports, cold-start checks, and the full test suite were completed.

Overall result: **PASS with two legacy-data caveats**.

1. Five real investigations contain 18 bounded typed relationships; four of five have at least one typed edge. Three older typed edges lack `source_page_id` provenance.
2. The current API and CLI community detectors agree on all five real graphs, but the HTTP graph endpoint returns `community_count: 0` for those legacy rows because their persisted investigation metadata has no community partition. This is a persistence/backfill gap, not an algorithm mismatch.

## Block 1 — pacing and boundary verification

The completed pacing/boundary section is retained here as the previously delivered portion of the audit. The current-head full-suite run re-executed its coverage.

**Result: PASS.** The pacing module remains the single source of truth for profiles, timeout/retry scaling, rolling-window limits, minimum spacing, and quota-protected delay floors. Boundary tests cover exact rolling-window edges, minimum spacing, retry plans, profile monotonicity, and the no-Tor path. Relevant coverage is in `tests/test_pacing.py`, `tests/test_pacing_enrichment.py`, `tests/test_pacing_quotas.py`, `tests/test_scrape_isolation.py`, and `tests/test_cli_proxy_config.py`.

## Block 2 — v2.0.1 blocker re-checks

### Sigma export on a real investigation

Using real investigation `b3c755bcb1d947369f9bf15612cfcbf0` from the local investigation store:

- CLI command exited `0`.
- Output was non-empty: `27,958` bytes.
- YAML validation found `49` Sigma documents/rules.

This re-check exercises the current v2.0.2 shared Sigma generator, including the v2.0.2 zero-byte regression fix.

### Exact stale-schema export reproduction

A fresh SQLite database was created from the ORM schema and then made stale by dropping `entities.investigation_count`. The exact CLI export path was then exercised with an explicit output path.

- Process exit: `1`.
- Message: `Database schema is stale; missing required column(s): entities.investigation_count...`.
- Output file: **not written**.

This confirms the requested fail-closed behavior: schema validation happens before export rendering or file creation.

**Result: PASS for both blocker re-checks.**

## Block 3 — typed-relationship yield on five real investigations

The following are five completed, non-empty investigations already present in the local store. Counts were read from persisted real investigation data; no synthetic entity fixtures are included in this table.

| Investigation | Pages | Entities | Typed edges | With source page | Yield |
|---|---:|---:|---:|---:|---|
| `b3c755…` | 19 | 262 | 10 | 10 | positive |
| `7aa20b…` | 20 | 278 | 4 | 4 | positive |
| `8babf3…` | 21 | 281 | 3 | 0 | positive; legacy provenance gap |
| `cfa514…` | 23 | 276 | 1 | 1 | positive |
| `06dfeb…` | 29 | 157 | 0 | 0 | zero typed yield |

Aggregate yield was **18 typed edges across 4/5 investigations**. Every edge was in the bounded vocabulary (`USES`, `DROPS`, `CONTROLS`, `TARGETS`, `EXPLOITS`, or `COMMUNICATES_WITH`). Fifteen of the 18 edges had a stored source page; the three missing page IDs are in the older `8babf3…` record.

The parser/compatibility and provenance contracts also passed `tests/test_relationship_extract.py` and the full suite.

**Result: PASS with a documented legacy provenance caveat.**

## Block 4 — live Tor ControlPort circuit-isolation proof

A temporary local Tor 0.4.9.6 instance was started with an isolated data directory, SOCKS5 on `127.0.0.1:19050`, ControlPort on `127.0.0.1:19051`, cookie authentication, and explicit `IsolateSOCKSAuth`. Tor bootstrapped to 100%.

ControlPort authentication and live stream evidence:

| Request | Credential source | STREAM circuit | HTTP result |
|---|---|---:|---|
| `www.iana.org` | `tor_socks_credentials("www.iana.org")` | 10 | `200 OK` |
| `www.rfc-editor.org` | `tor_socks_credentials("www.rfc-editor.org")` | 6 | `301 Moved Permanently` |
| repeat `www.iana.org` | same credential as first request | 10 | `200 OK` |

The ControlPort returned `515 Authentication failed` for unauthenticated access and accepted the generated cookie. The actual `650 STREAM ... SUCCEEDED ...` events carried the username/password fields and circuit IDs shown above. The two distinct credentials isolated, and same-credential reuse held. The temporary Tor process was stopped after the proof.

**Result: PASS, live-verified.**

## Block 5 — entity prioritization and live API parity

The live API request `GET /investigations/b3c755…/entities?limit=100&min_confidence=0&defang=false` returned HTTP `200`, 100 items, and a total of 262 entities. The CLI adapter returned all 262 entities.

- IDs compared: 100 API items against the corresponding CLI rows.
- `priority_score` mismatches: `0`.
- Component and centrality fields were present and matched on the compared rows.

The synthetic DB contract test `tests/test_priority_score_cli_api_parity.py` and the entity-priority unit tests also passed.

**Result: PASS.**

## Block 6 — community detection CLI/API parity

The shared detector and API adapter were run against the same five real investigations. The CLI UUID-keyed partition and the API canonical-node partition were translated to the same entity scope and compared by grouping.

| Investigation | Graph nodes | Graph edges | Entity keys equal | Grouping equal |
|---|---:|---:|---|---|
| `b3c755…` | 255 | 212 | yes | yes |
| `7aa20b…` | 272 | 556 | yes | yes |
| `8babf3…` | 274 | 556 | yes | yes |
| `cfa514…` | 269 | 553 | yes | yes |
| `06dfeb…` | 157 | 309 | yes | yes |

The HTTP graph endpoint returned `200`/`complete` for all five, but reported `community_count: 0` because those legacy investigations have empty persisted `metadata` and no saved `communities` map. The CLI recomputation and API algorithm both produced non-empty, identical partitions. A fresh pipeline run that persists the partition should be used to close the remaining endpoint-data caveat.

**Result: PASS for detector parity; conditional for legacy persisted API metadata.**

## Block 7 — exports

All requested export formats were run through the CLI against real investigation `b3c755…`. Every command exited `0` and wrote a non-empty artifact.

| Format | Bytes | Validation |
|---|---:|---|
| STIX | 178,124 | JSON bundle with 240 objects |
| MISP | 122,985 | JSON event with 141 attributes |
| YARA | 25,583 | non-empty rules file; 716 non-comment lines |
| Snort | 6,581 | non-empty rules file; 208 non-comment lines |
| Suricata | 9,398 | non-empty rules file; 289 non-comment lines |
| Sigma | 27,958 | 49 YAML documents/rules |
| IOC package | 75,165 | ZIP with 21 entries; all required entries non-empty |

The IOC package contained non-empty `threat_intel/stix.json`, `threat_intel/misp.json`, `detections/sigma.yml`, `detections/yara.yar`, `detections/snort.rules`, `detections/suricata.rules`, `reports/entities.csv`, and `reports/summary.md`.

**Result: PASS.**

## Block 8 — cold start and full test suite

Fresh CLI processes were used for the cold-start-safe paths:

- `python -m voidaccess_cli.main --version`: passed, about 6.6s.
- `python -m voidaccess_cli.main --no-banner --help`: passed, about 7.8s.

The full suite was run with `TEMP`/`TMP` redirected to a workspace-owned temporary directory because the default Windows pytest temp root was ACL-blocked (`WinError 5`). That harness workaround changed no product code.

```text
1291 passed, 7 warnings in 301.13s (0:05:01)
```

There were no test failures or errors. The seven warnings were dependency/runtime warnings: two Google API Python-version notices, four mocked email-reputation coroutine warnings, and one mocked cache coroutine warning.

**Result: PASS.**

## Final disposition

The requested eight-block evidence is now complete. The v2.0.1 blockers are closed by direct reproduction, and the previously outstanding Tor proof is closed by live ControlPort `STREAM` evidence.

Remaining follow-up is limited to data hygiene for older records:

- backfill or explicitly mark missing `source_page_id` on the three legacy typed edges;
- persist/backfill community partitions for legacy completed investigations so the HTTP graph endpoint exposes the same non-empty partition that the current CLI/API detector computes.
